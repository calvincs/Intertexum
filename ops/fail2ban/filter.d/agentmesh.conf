# Application emits this fixed line only for a source-IP automatic ban.
# Never match advertised endpoints, exception text, peer claims or query content.
[Definition]
failregex = ^agentmesh abuse source=<HOST>$
ignoreregex =
usedns = no
